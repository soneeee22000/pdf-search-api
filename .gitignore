# The seven PDFs and the brief are both published, so that everything this
# README claims can be checked against the inputs it was measured on.
# Local pre-flight checklist, not part of the deliverable.
RUNBOOK.md
storage/
storage.*/
storage.staging/
*.faiss

.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
build/
dist/

models/
.env
.DS_Store
